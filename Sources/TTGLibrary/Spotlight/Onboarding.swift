//
//  Onboarding.swift
//  TurkeyTouristGuide
//
//  Created by Serhat Akkurt on 16.02.2024.
//

import Foundation
/*
enum Onboarding {
    case home, placeList, placeDetail, placeDetailWithTicket, map

    var value: Spotlight {
        switch self {
            case .home:
                return Spotlight(
                    elements: [
                        Spotlight.Element(key: Keys.Home.search, message: L10n.Tutorial.Home.search),
                        Spotlight.Element(key: Keys.Home.category, message: L10n.Tutorial.Home.category),
                        Spotlight.Element(key: Keys.Home.city, message: L10n.Tutorial.Home.city),
                        Spotlight.Element(key: Keys.Home.route, message: "tutorial.home.route".localize),
                    ],
                    cancellable: true
                )
            case .placeList:
                return Spotlight(
                    elements: [
                        Spotlight.Element(key: Keys.PlaceList.search, message: L10n.Tutorial.PlaceList.search),
                        Spotlight.Element(key: Keys.PlaceList.filter, message: L10n.Tutorial.PlaceList.filter),
                        Spotlight.Element(key: Keys.PlaceList.sort, message: "tutorial.placeList.sort".localize),
                        Spotlight.Element(key: Keys.PlaceList.map, message: L10n.Tutorial.PlaceList.map),
                        Spotlight.Element(key: Keys.PlaceList.quickFilter, message: L10n.Tutorial.PlaceList.quickFilter),
                    ],
                    cancellable: true
                )
            case .placeDetail:
                return Spotlight(
                    elements: [
                        Spotlight.Element(key: Keys.PlaceDetail.like, message: L10n.Tutorial.PlaceDetail.like),
                        Spotlight.Element(key: Keys.PlaceDetail.visited, message: L10n.Tutorial.PlaceDetail.visited),
                        Spotlight.Element(key: Keys.PlaceDetail.map, message: L10n.Tutorial.PlaceDetail.map),
                    ],
                    cancellable: true
                )
            case .placeDetailWithTicket:
                return Spotlight(
                    elements: [
                        Spotlight.Element(key: Keys.PlaceDetail.like, message: L10n.Tutorial.PlaceDetail.like),
                        Spotlight.Element(key: Keys.PlaceDetail.visited, message: L10n.Tutorial.PlaceDetail.visited),
                        Spotlight.Element(key: Keys.PlaceDetail.map, message: L10n.Tutorial.PlaceDetail.map),
                        Spotlight.Element(key: Keys.PlaceDetail.ticket, message: L10n.Tutorial.PlaceDetail.ticket),
                    ],
                    cancellable: true
                )
            case .map:
                return Spotlight(
                    elements: [
                        Spotlight.Element(key: Keys.Map.filter, message: L10n.Tutorial.Map.filter),
                        Spotlight.Element(key: Keys.Map.shortcut, message: L10n.Tutorial.Map.shortcut),
                        Spotlight.Element(key: Keys.Map.pins, message: L10n.Tutorial.Map.pins),
                        Spotlight.Element(key: Keys.Map.pinBox, message: L10n.Tutorial.Map.pinBox),
                    ],
                    cancellable: true
                )
        }
    }

    enum Keys {
        enum Home {
            static let search = "Home.search"
            static let category = "Home.category"
            static let city = "Home.city"
            static let route = "Home.route"
        }

        enum PlaceList {
            static let search = "PlaceList.search"
            static let filter = "PlaceList.filter"
            static let sort = "PlaceList.sort"
            static let map = "PlaceList.map"
            static let quickFilter = "PlaceList.quickFilter"
        }

        enum PlaceDetail {
            static let like = "PlaceDetail.fav"
            static let visited = "PlaceDetail.seen"
            static let map = "PlaceDetail.map"
            static let ticket = "PlaceDetail.ticket"
        }

        enum Map {
            static let filter = "Map.filter"
            static let shortcut = "Map.shortcut"
            static let pins = "Map.pins"
            static let pinBox = "Map.pinBox"
        }
    }
}
*/
